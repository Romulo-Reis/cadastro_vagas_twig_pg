{% extends '@layouts/base.html.twig' %}

{% block conteudo %}
<section id="main-content">
    <section class="wrapper">
        <div class="row">
            {% if mensagem is not empty %}
                <div class="col-lg-12">
                    <div class="alert alert-success" role="alert">
                        <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
                        <b>{{ mensagem }}</b>
                    </div>
                </div>
            {% endif %}
            <div class="col-lg-12">
                <section class="panel panel-default">
                    <header class="panel-heading">
                        Tecnologias disponíveis
                    </header>
                    <div class="panel-body">
                        <table id="lista-tecnologias" class="table table-striped table-hover">
                            <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>Tecnologia</th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for tecnologia in tecnologias %}
                                <tr>
                                    <td>{{ tecnologia.idtecnologia }}</td>
                                    <td>{{ tecnologia.tecnologia }}</td>
                                    <td>
                                        <a href="http://{{ APP_HOST }}/tecnologia/edicao/{{ tecnologia.idtecnologia }}"><i class="fa fa-pencil"></i></a>
                                        <a href="http://{{ APP_HOST }}/tecnologia/exclusao/{{ tecnologia.idtecnologia }}"><i class="fa fa-trash-o"></i></a>
                                    </td>
                                </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </section>
            </div>
        </div>
    </section>
</section>
{% endblock %}